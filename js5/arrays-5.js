let arr = [
    "Алимбек",
    "Хасамбек",
    "Адлан",
    "Жовжан",
    "Сапият",
    "Султан",
    "Улумбек",
    "Сайхан",
    "Жамлай",
    "Висик"
];

for (let i = 0; i < arr.length; i++) {
    if (arr[i].startsWith("А") || arr[i].startsWith("а")) {
        // console.log(arr[i]); 
    }
}

// for (let i = 0; i < arr.length; i++) {
//     if (arr[i].toLowerCase().startsWith('а')) {
//         console.log(arr[i]);
//     }
// }