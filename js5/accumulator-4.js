const arr = [
    "Ромли",
    "Рошид",
    "Холид",
    "Хьасан",
    "Адам",
    "Адлан",
    "Алина",
    "Амина",
    "Амалия",
    "Хьамид"
];

let accumulator = [];

for (let i = 0; i < arr.length; i++) {
    if (arr[i].startsWith("А") || arr[i].startsWith("а")) {
        // console.log(arr[i]);
    }
}